# kvn-node-api

Simple Node.js Express API used as a DevOps / SRE learning project.

## Endpoints

- `/`  
  Returns a small HTML greeting from KVN DevOps Node API.

- `/health`  
  Returns JSON health status:

  ```json
  { "status": "ok" }
